# 介護者向け LINE Bot — システム設計書（MVP版）

**ver 1.0 | 2026年4月**

---

## 1. プロジェクト概要

介護をする家族（40〜60代）を対象に、日々の介護記録をLINEで手軽に残せるBotを開発する。会話形式で体調・服薬・食事などを記録し、AIが週次サマリーや気になるパターンをフィードバックすることで、介護者の負担軽減と医師への情報共有を支援する。

| 項目 | 内容 |
|---|---|
| サービス名 | 介護メモBot（仮） |
| 対象ユーザー | 在宅介護をしている家族（40〜60代） |
| プラットフォーム | LINE Bot（将来的にPWAへ昇格予定） |
| フェーズ | MVP（最小限の機能で検証） |
| 開発者 | 個人開発（副業） |

---

## 2. MVP機能定義

### 対象機能（このフェーズで作るもの）

- 体調・出来事をLINEテキストで送ると記録される
- 「今週まとめて」と送ると、週間サマリーをAIが生成して返信
- 気になるパターン（例：混乱が週3回以上）を自動検知してコメント

### 対象外機能（将来フェーズ）

- 医師への相談文の自動生成
- 感情サポートチャット
- 音声入力対応
- 家族間での記録共有
- 有料プラン・サブスク課金

---

## 3. 技術スタック

| レイヤー | 技術 | 備考 |
|---|---|---|
| フロントエンド | LINE Bot | Messaging API使用 |
| バックエンド | Python (AWS Lambda) | サーバーレス構成 |
| API接続 | AWS API Gateway | LINEからのWebhookを受け口 |
| データベース | Amazon DynamoDB | 無料枠：25GB / 200万リクエスト/月 |
| AI API | Claude Haiku 4.5 | $0.08/100万トークン（入力） |
| ファイルストレージ | 不要（MVP段階） | - |

---

## 4. システム構成

### アーキテクチャ概要

```
ユーザー（LINEアプリ）
    ↓ メッセージ送信
LINE Platform
    ↓ Webhook
API Gateway
    ↓
Lambda（Python）
    ↓               ↓
DynamoDB       Claude API（Haiku 4.5）
    ↓
LINE Platform
    ↓ 返信
ユーザー（LINEアプリ）
```

### 処理フロー

| ステップ | 処理内容 |
|---|---|
| ① ユーザー送信 | LINEアプリでメッセージを送信 |
| ② Webhook受信 | LINE Platform → API Gateway → Lambda |
| ③ 意図解析 | LambdaがClaude APIに問い合わせ（記録 or サマリー要求か判定） |
| ④ DB操作 | 記録の場合：DynamoDBに保存 / サマリーの場合：過去記録を取得 |
| ⑤ 返信生成 | Claude APIが返信文を生成 |
| ⑥ LINE返信 | LambdaがLINE Messaging API経由でユーザーに返信 |

---

## 5. データ設計

### DynamoDB テーブル設計

テーブル名：`KaigoRecords`

| 属性名 | 型 | 役割 |
|---|---|---|
| userId (PK) | String | LINEのユーザーID |
| timestamp (SK) | String | 記録日時（ISO 8601形式） |
| rawMessage | String | ユーザーが送ったテキスト（原文） |
| summary | String | AIが要約した記録テキスト |
| tags | List | 抽出したタグ（混乱・服薬・食事など） |
| createdAt | String | レコード作成日時 |

### 会話コンテキスト管理

各リクエストでDynamoDBから直近7日分の記録を取得し、Claude APIへ渡す。セッション管理は行わない（ステートレス設計）。

---

## 6. LINE Bot フロー設計

### 記録フロー

1. ユーザーが自由なテキストで送信
   - 例：「今日お母さん昼ごはん半分、夕方少し混乱してた。薬は飲んだ」
2. LambdaがClaudeで内容を構造化（日時・症状・食事・服薬を抽出）
3. DynamoDBに保存
4. 「記録しました。今週3回混乱の様子が見られます」のように返信

### サマリーフロー

1. ユーザーが「今週まとめて」「先週の記録」などと送信
2. DynamoDBから該当期間の記録を取得
3. ClaudeがMarkdown形式で週次レポートを生成
4. LINEに返信（長い場合は分割送信）

### エラー・免責ポリシー

- 医療的判断を求めるメッセージには「私はAIです。医療アドバイスはできません。かかりつけ医にご相談ください」と必ず返す
- 曖昧なメッセージは記録の促しに誘導する

---

## 7. コスト試算

| 項目 | 無料枠 | 想定コスト（10ユーザー/月） |
|---|---|---|
| LINE Messaging API | 200通/月 | 0円（無料枠内） |
| AWS Lambda | 100万リクエスト/月 | 0円（無料枠内） |
| AWS API Gateway | 100万リクエスト/月 | 0円（無料枠内） |
| DynamoDB | 25GB / 200万リクエスト | 0円（無料枠内） |
| Claude Haiku 4.5 API | なし（従量課金） | 約50〜200円/月 |
| **合計** | - | **約50〜200円/月** |

---

## 8. 開発ロードマップ

| フェーズ | 内容 | 目安期間 |
|---|---|---|
| Phase 0 | LINE DevelopersでBotチャンネル作成・トークン取得 | 1日 |
| Phase 1 | ローカル + ngrokでオウム返しBotを動かす | 1〜2日 |
| Phase 2 | Claude API連携・記録機能の実装 | 3〜5日 |
| Phase 3 | DynamoDB連携・保存・サマリー機能 | 3〜5日 |
| Phase 4 | AWS Lambda + API Gatewayへ移行 | 2〜3日 |
| Phase 5 | 知人1〜2名でベータテスト・フィードバック | 1〜2週間 |

---

## 9. 今後の検討事項

- プライバシーポリシー・利用規約の作成（LINEチャンネル審査に必要）
- 個人情報（医療情報）の暗号化・削除ポリシーの策定
- 有料プランの設計（月額500〜980円のサブスクを想定）
- ユーザー増加時のSonnetへのモデル切り替え検討
- PWAへの昇格タイミングの判断基準設定
