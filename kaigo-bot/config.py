import os

# --- LINE ---
LINE_CHANNEL_ACCESS_TOKEN = os.environ["LINE_CHANNEL_ACCESS_TOKEN"]
LINE_CHANNEL_SECRET = os.environ["LINE_CHANNEL_SECRET"]
MAINTENANCE_MODE = os.environ.get("MAINTENANCE_MODE", "false").lower() == "true"
MAINTENANCE_MESSAGE = "現在メンテナンス中です。しばらくお待ちください🙏"

# --- Anthropic ---
ANTHROPIC_API_KEY = os.environ["ANTHROPIC_API_KEY"]
CLAUDE_MODEL = "claude-haiku-4-5-20251001"
CLAUDE_MAX_TOKENS = 1000

# --- DynamoDB ---
DYNAMODB_REGION = "ap-northeast-1"
DYNAMODB_TABLE_NAME = "KaigoRecords"

# --- S3 (CSV export) ---
S3_BUCKET_NAME = os.environ.get("S3_BUCKET_NAME", "")
S3_PRESIGNED_URL_EXPIRY = 3600  # 1時間

# --- Period keywords (summary) ---
# 新しい期間を追加するときはここだけ変更する
PERIOD_MAP = {
    "今日のまとめ": "今日",
    "今週のまとめ": "今週",
    "今月のまとめ": "今月",
}

# --- Category prompt triggers ---
# 新しいカテゴリを追加するときはここだけ変更する
# value: (ユーザーへの促しメッセージ, DBに保存するカテゴリ名)
CATEGORY_PROMPTS = {
    "体調を記録": ("体調について教えてください。\n例：「朝から少し熱っぽい」「機嫌が良かった」", "体調"),
    "薬を記録":   ("お薬について教えてください。\n例：「朝の薬を飲ませた」「夜の薬を飲み忘れた」", "薬"),
    "食事を記録": ("食事について教えてください。\n例：「朝ごはん全部食べた」「水分少なめ」", "食事"),
}

# --- 自然言語・表記ゆれ対応 ---
# サマリー意図を示すキーワード（期間キーワードと組み合わせて判定）
SUMMARY_INTENT_KEYWORDS = [
    "まとめ", "サマリー", "さまりー", "様子", "ようす",
    "どうだった", "どうだっ", "見せて", "みせて", "教えて", "おしえて",
    "振り返り", "ふりかえり", "どう", "レポート", "れぽーと",
]

# 期間キーワード → 正規キーワードのマッピング
FUZZY_SUMMARY_PERIODS = {
    "今日のまとめ": ["今日", "きょう", "本日", "ほんじつ"],
    "今週のまとめ": ["今週", "こんしゅう", "先週", "せんしゅう"],
    "今月のまとめ": ["今月", "こんげつ", "先月", "せんげつ"],
}

# トップメニューのキーワードゆれ → 正規キーワードのマッピング
FUZZY_TOP_MENU = {
    "まとめ": ["まとめ", "サマリー", "さまりー", "振り返り", "ふりかえり"],
    "記録": ["記録", "きろく"],
    "使い方": ["使い方", "つかいかた", "ヘルプ", "へるぷ", "help"],
    "相談文": ["相談文", "診察メモ", "診察", "相談"],
    "エクスポート": ["エクスポート", "export", "CSV", "csv", "書き出し"],
    "記録を検索": ["記録を検索", "検索", "探す", "さがす"],
}

SYSTEM_PROMPT = """あなたは介護者をサポートするAIアシスタントです。
ユーザーは在宅介護をしている家族です。

以下のルールを守ってください：
- 介護の記録を受け取ったら内容を要約して「記録しました」と返す
- 主語が省略されている場合は要介護者（ご本人）の話として解釈する
- 返答には「ご本人が〜」のように主語を補って返す
- 医療的なアドバイスは絶対にしない
- 「医療アドバイスはできません。かかりつけ医にご相談ください」と伝える
- 常にやさしく、寄り添うトーンで返答する
- 返答は短く簡潔に（LINEなので長文は避ける）"""

SUMMARY_SYSTEM_PROMPT = """あなたは介護者をサポートするAIアシスタントです。
過去の介護記録を受け取り、読みやすくやさしくまとめてください。

以下のルールを守ってください：
- 「記録しました」などの定型文は使わない
- マークダウン記法（**や#や-など）は絶対に使わない。プレーンテキストのみ
- 箇条書きは「・」を使う
- 複数日の記録がある場合は日付ごとに整理して記載する
- 各日の要点を1〜2文でまとめる。記録がない日は省略する
- 常にやさしく、寄り添うトーンで返答する
- 医療的なアドバイスは絶対にしない"""

CONSULT_SYSTEM_PROMPT = """あなたは介護者をサポートするAIアシスタントです。
以下の介護記録をもとに、医師への相談に役立つ報告文を作成してください。

【フォーマット（厳守）】
1行目は必ず「医師への相談報告文」とする。

カテゴリの見出しは【バイタル】【体調】【食事】【薬】の形式で記載する。記録がないカテゴリは省略する。

各カテゴリ内は以下の形式で日付ごとに記載する：
・月日：内容

各カテゴリの最後に、変化・繰り返しパターン・気になる点を1〜2文で追記する。

最後に【気になった点】として全体を通じた気になる点を1〜2文でまとめる。

【その他のルール】
- マークダウン記法（**や#や-など）は絶対に使わない。プレーンテキストのみ
- 体温の単位は必ず「℃」を使う
- 医療的な診断や判断はしない。事実のみ記載する
- 「〜でした」「〜がありました」のような客観的な文体で書く"""

# --- 相談文の期間設定 ---
CONSULT_PERIOD_MAP = {
    "相談文（2週間）": 14,
    "相談文（1ヶ月）": 30,
    "相談文（3ヶ月）": 90,
}
CONSULT_MAX_TOKENS = 2000

HELP_MESSAGE = """📖 ケアサポの使い方

【記録する】
「記録」ボタンから種類を選んで送るだけ！
🌡 バイタル → 体温・血圧・脈拍を1つずつ入力（スキップも可）
🩺 体調　　→「朝から少し熱っぽい」
💊 薬　　　→「朝の薬を飲ませた」
🍚 食事　　→「朝ごはん全部食べた」

💡 主語をつけると後で見返しやすいです
例：「お母さんが朝ごはん全部食べた」
※ 省略した場合はご本人の記録として扱います

📅 別の日付で記録したいときは
「記録」→「📅 別の日」でカレンダーから選択

【まとめを見る】
「まとめ」ボタンで期間を選択
📅 今日 / 📊 今週 / 📆 今月
🗓 期間指定 → 開始日・終了日をカレンダーで選択（1年以内）

【医師への相談文を作る】
「相談文」と送ると期間を選んで報告文を生成します

【記録をCSVで書き出す】
「エクスポート」と送ると期間を指定してCSVをダウンロードできます

【特定日の記録を確認する】
「記録を検索」と送ると日付を指定してその日の記録を一覧表示します

⚠️ 医療的な判断は必ずかかりつけ医にご相談ください"""
